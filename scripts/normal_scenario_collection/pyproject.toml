[project]
name = "ott-normal-scenario-collection"
version = "1.0.0"
description = "Normal scenario data collection for OTT detection system"
requires-python = ">=3.9"
dependencies = []

[tool.uv]
python-version = "3.11"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
